on:
 push:
   branches: ["main"]
      
jobs:
  print-username:
    runs-on: ubuntu-latest

    steps:    
    - name: 📂 Sync files
      uses: SamKirkland/FTP-Deploy-Action@v4.3.4
      with:
        server:  ${{ HOST_FTP }}
        username:  ${{ USER_FTP }}
        password: ${{ secrets.PASSWORD_FTP }}
        

  

      
     

  

      
